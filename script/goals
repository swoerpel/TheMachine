goals
-- display params left click option
-- 'move to base' right click option
-- randomize base params

-- randomize offset matrices (keep base params)
    offset matrices -> 2
    horizontal and vertical diretions only


-- create grid of IFS
    - base params (top left corner)
    - each row is new offset matrix


order of operations
- create grid of random params
- display params which are clicked

var trad_ifs_params = {
    zoom:{x:1,y:1},
    stroke_weight:2,
    iterations_per_draw: 100,
    load: '', //rand if blank
    variance: 1, // +- params can be from zero
    function_count: 2,
    constant_count: 6
    // load: 'floor_spikes', //rand if blank
}

